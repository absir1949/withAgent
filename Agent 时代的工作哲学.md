# Agent 时代的工作哲学

## 背景

随着 Agent 能力增强，一个任务可能自主运行 20～30 分钟甚至更久。等待浪费人的时间，但简单增加并发，又会让人的注意力和验收能力成为新的瓶颈。

因此，真正需要优化的逐渐不再是「AI 做得有多快」或「同时跑多少 Agent」，而是：**如何减少机器对人类判断与注意力的占用，让 Agent 在正确方向上自主工作更久。**

这套工作哲学由此产生。

## 目标函数

**让每一单位稀缺的人类判断，撬动尽可能多的有效机器劳动。**

这里的「有效」，不是机器做了多少事情、运行了多少小时，而是它是否朝人所定义的价值方向前进，并且经得起外部约束的验证。

Agent 时代真正稀缺的资源，正在从「执行时间」转向「人的判断与注意力」。

---

## 一、价值主权

**机器可以参与一切判断，但人保留价值函数的最终所有权。**

AI 可以分析、质疑、提出反方意见，甚至在很多具体判断上比人做得更好。但「什么值得做」「什么结果是好的」「增长、风险、速度、体验之间如何取舍」，最终仍然是价值选择。

执行越便宜，选错方向的代价反而越大。

过去方向错了，可能浪费一个人一天；未来方向错了，可能让十个 Agent 在一晚上高效地浪费几十个机器小时。

所以最高价值的人类判断，首先是：

**做什么，以及为什么做。**

---

## 二、探索购买信息

**探索的目的不是生产，而是购买信息。实现正在从资产变成耗材。**

当实现越来越廉价，我们不应该再用「留下了多少代码」衡量探索，而应该问：

**这次探索让我知道了什么？**

做一个 Prototype、验证一个技术点、同时实现 A/B/C 三个版本，甚至最后全部删除，只要它帮助我们确认或排除了某种可能性，就是有效探索。

因此探索阶段允许一个快速循环：

**判断 → 实验 → 获得新信息 → 更新判断。**

看到结果以后产生新的判断，并不意味着最初定义失败。很多信息本来就只有看到东西真正运行起来之后才能获得。

一旦探索已经获得足够信息，问题进入确定状态，就应该停止这种循环，把已经形成的判断编码成目标、边界与验收标准，交给机器执行。

**探索期追求信息增益，确定期追求自主执行。**

---

## 三、反馈闭环

**自治来自反馈，而不是信任。**

一个任务能够交给 Agent 多久，不只取决于目标是否清楚，还取决于机器能不能知道自己有没有做对。

好的验证，不是问执行者：

> 你觉得自己做对了吗？

而是让结果不断撞上一个**执行者无法随意解释的外部约束**：既有测试、编译器、明确规则、预先确定的验收标准、截图基准、真实运行结果、外部数据等。

因此，我们真正追求的不是让机器长时间「没有反馈」，而是：

**让机器频繁接受机器可以获得的反馈，却尽可能少地需要人的反馈。**

反馈的三个重要属性——**时延、客观性、成本**——决定了一件事情能够达到多高程度的自治。

反馈越快、越客观、越便宜，Agent 越适合长时间自治；反馈越慢、越主观、越昂贵，人就越需要留在循环中。

「偏航半径」可以作为这一原则的工程指标：

**两次可靠反馈之间，机器最多能够错多远？**

理想状态不是 Agent 八小时不犯错，而是它在八小时里犯错、验证、纠偏无数次，却一次都不需要人介入。

---

## 四、认知背压

**机器的产出不能长期超过人的判断吞吐量。**

机器可以高度并发，人类判断不能。

如果晚上同时运行二十个 Goal，第二天产生二十个 PR、二十份报告、二十段录屏，而人需要一整天才能验收，那么系统并没有真正获得二十倍生产力。

它只是把库存从「待开发」转移成了「待验收」。

因此，成熟的 Agent 系统不应该追求机器永远满负荷，而应该主动限制在制品数量，让机器的产出速度与人的最终判断能力匹配。

**机器闲置很便宜，人类认知过载很昂贵。**

---

## 二级推论

以上四条可以自然推出一些工作原则，但它们不作为独立哲学原则存在。

- **机器负责并发，人负责专注。** 不需要训练自己同时盯十个 Agent，而应该让 Agent 并行工作，人仍然保持相对串行的思考。

- **判断尽可能前置。** 能在定义阶段编码成目标、约束和验收标准的判断，不要留到执行过程中反复支付。

- **延长 Human Intervention Interval。** 真正值得优化的不只是 Agent 从 30 分钟加速到 20 分钟，而是一次人类判断之后，它能正确自主运行多久。

- **不要让机器调度人的注意力。** Agent 可以事件驱动，人应该批处理认知。机器什么时候完成是机器的事情，人什么时候处理结果是人的事情。

- **Agent 只有越出自身判断边界时，才应该请求人类介入。** 主要有两种情况：遇到了需要人决定「什么才是好的」的价值问题；或者遇到了无法通过现有反馈闭环验证的问题。其他情况下，应优先自行验证、纠偏和继续执行。

- **睡眠可以成为机器的自治窗口。** 人的离线不再意味着生产停止。只要反馈闭环足够强，人的离线时间就是天然的长时间机器执行窗口。

- **在反馈慢的领域，杠杆主要来自提高探索密度和判断质量，而不是延长自治时间。** 当真实反馈需要数天、数周甚至更久才能获得时，AI 更大的价值可能是帮助人提出更多假设、更低成本地验证方向和做出更好的判断。

---

## 总结

**人的一次高质量判断，能让机器在正确方向上自主前进多远。**

---

v0.1
