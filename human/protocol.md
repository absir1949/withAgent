# Agent 工作协议

## **背景**

Agent 可以连续工作几十分钟甚至数小时，新的瓶颈不再只是执行速度，而是人的注意力被完成通知、问题反馈和即时修改不断打断。

工作方式应从「人与 Agent 实时协作」转向「人集中判断，机器异步执行」。

**核心目标：减少认知切换，让人保持专注，让 Agent 获得完整的自主执行时间。**

---

## **一、基本循环**

人的工作分为四个阶段：

**探索 → 定义 → 执行 → 验收**

### **探索**

回答：**我们到底应该怎么做？**

可以讨论、搜索、分析、实验、制作 Prototype、比较方案。探索的产出是信息和判断，而不是最终代码。关键不确定性消除后，进入定义。

### **定义**

回答：**接下来要机器完成什么？**

把判断编码为计划，至少包含：目标、边界、约束、验收标准和验证方式。仍依赖未知前置结果的任务，还没有定义完成。

### **执行**

执行主要属于机器。前置条件确定且相互独立的任务可以并行。

任务下发后，人默认退出：**不看进度，不因完成通知立即查看，不临时修改。**

Agent 只有在两种情况下请求人判断：遇到价值问题，或现有反馈无法验证。遇到阻塞时，应记录问题和假设，继续处理不依赖该判断的部分；只有整个任务无法继续时才等待。

### **验收**

在预定窗口集中查看一批结果。验收阶段可以分析和讨论，但默认不直接修改；除非预先定义了紧急例外。

验收通过，任务结束；验收失败，进入必要的探索、重新定义和下一轮执行。

---

## **二、两个交接接口**

### **人 → Agent：计划**

计划把人的判断编码成机器可以自主执行的任务，至少说明：

- 要完成什么；
- 什么不做；
- 有哪些约束；
- 怎样算完成；
- 怎样自行验证。

### **Agent → 人：交付物**

交付物不应只报告「已完成」，而应降低验收成本，至少包含：

- **改动摘要**；
- **验证结果**；
- **不确定点与决策记录**；
- **重点验收项**；
- **最短验收路径**。

UI、App 等任务还可以提供截图、录屏、构建结果或自动化测试结果。

**Agent 不只是完成任务，也负责降低人判断任务是否完成的成本。**

---

## **三、批次与认知阶段**

Agent 可以事件驱动，人不应被事件驱动。

**任务完成，不等于人应该立即介入。**

预先设置验收窗口，两次窗口之间默认不查看执行中的任务。具体时间可以调整，固定的是批量处理而不是即时响应。

探索、定义、执行、验收尽量不混在一起：

- 探索时不急着实现；
- 定义时不重新无限发散；
- 执行时人退出循环；
- 验收时先形成判断，不被单个问题拖入即时修改。

一个任务即使已经完成，也可以等待到验收窗口。**机器等待很便宜，人的注意力切换很贵。**

机器执行当前批次时，人探索和定义下一批：

**机器：执行 N → 执行 N+1**

**人：定义 N+1 → 验收 N → 定义 N+2 → 验收 N+1**

只有前置条件已经确定的任务，才能提前进入下一批；依赖上一批结果决定方向的任务，继续留在探索阶段。

---

## **四、失败与并发**

第一次执行不必一次成功。失败应进入异步的下一轮，而不是即时修复循环：

**失败结果 → 分析原因 → 必要时重新探索 → 重新定义 → 形成计划 → 下一轮执行。**

当前批次完成验收、问题完成定义后，就可以下发下一轮，不必等待固定钟点。需要等待的是人的认知阶段完成，而不是某个时间点。

并发量由人的验收能力决定，而不是机器是否空闲：

**一批任务预计产生的验收工作，不应超过下一个验收窗口的容量。**

机器闲置不是问题，**验收债务才是问题。**

---

## **五、v0.1 运行与观察**

当前采用：

**探索和定义 → 批量下发 → 执行期间不看 → 集中验收 → 失败项进入下一轮。**

睡眠是天然的无人值守执行窗口。白天 Agent 执行时，人主要探索未来的问题，形成下一批高确定性的计划。

暂时只观察四个指标：

- **一次验收通过率**；
- **平均返工轮数**；
- **中途阻塞次数**；
- **验收耗时**。

另外记录：**我今天被 Agent 打断了多少次？**

这些指标不是为了考核 Agent，而是为了发现协议问题：通过率低可能是定义不足，返工多可能是反馈不足，阻塞多可能是边界或默认策略不足，验收耗时增长则说明出现了认知背压。

---

## **一句话**

**人按认知阶段工作，机器按任务并发工作；通过计划与交付物异步交接，通过批量验收完成判断闭环。**

---

v0.1
