# Agent 工作协议

## **背景**

Agent 已经可以独立工作几十分钟甚至数小时。新的瓶颈不再只是机器执行速度，而是人的注意力不断被任务完成、问题反馈和即时修改所打断。

因此，工作方式需要从「人与 Agent 实时来回协作」，转向「人集中判断，机器异步执行」。

核心目标是：

**减少认知切换，让人保持专注，让 Agent 获得尽可能完整的自主执行时间。**

---

## **一、工作分为四种状态**

人的工作明确分为四种状态：

**探索 → 定义 → 执行 → 验收**

不同状态解决不同问题，尽量不要混在一起。

### **1. 探索**

探索只解决：

**我们到底应该怎么做？**

可以与 AI 讨论、搜索资料、分析代码、做实验、写 Prototype、比较多个方案。

探索允许发散，也允许推翻之前的想法。它的产出不是最终代码，而是**信息和判断**。

当关键不确定性已经消除，停止探索，进入定义。

### **2. 定义**

定义只解决：

**接下来到底要机器完成什么？**

把已经形成的判断转换成一份尽可能确定的执行计划，包括：

- 目标
- 边界
- 约束
- 验收标准
- 验证方式

一个好的计划，应该让 Agent 在执行以后尽可能长时间不需要回来询问人。

如果一个任务仍然依赖尚未得到的前置结果，它就还不是一个确定任务，不应该过早进入执行队列。

定义完成以后，任务进入执行队列。

### **3. 执行**

执行主要属于机器。

相互独立、前置条件已经确定的计划，可以通过不同分支、Worktree 或环境并行执行。

任务一旦下发，人默认退出：

**不看进度，不因为某个 Agent 完成而立即查看，不在执行过程中临时产生修改。**

Agent 只有在两类问题上需要请求人的判断：

- 遇到价值问题，无法确定「什么才是好的」；
- 遇到现有反馈机制无法验证的问题。

但：

**请求人类判断，不等于停止工作。**

遇到阻塞时，Agent 应该优先：

1. 记录问题以及需要人决定的内容；
2. 如果存在低风险、可逆的默认方案，明确记录假设后继续；
3. 如果当前部分无法继续，切换到不依赖该判断的其他部分；
4. 在交付时明确标记这个决策点。

只有当整个任务确实无法继续时，才进入等待。

### **4. 验收**

验收是一个独立的认知阶段。

到达预定验收窗口以后，再集中查看这一批任务的结果。

验收过程中可以与 AI 充分交流：分析问题、解释实现、检查结果、寻找原因、讨论替代方案。

但是：

**验收阶段默认不直接修改。**

发现问题以后，先形成判断并记录，不立即进入「改一下 → 等待 → 再看」的即时循环。

即使问题看起来只需要两分钟修复，也默认先完成当前批次的验收。

验收通过的任务结束。

验收不通过的任务重新进入：

**必要的探索 → 重新定义 → 形成新计划 → 下一轮执行。**

---

## **二、两个交接接口**

人与 Agent 之间尽量不通过持续聊天维持工作，而通过两个明确接口完成交接。

### **人 → Agent：计划**

计划负责把人的判断编码成机器可以自主执行的任务。

至少应该说明：

- 要完成什么；
- 什么不做；
- 有哪些约束；
- 怎样算完成；
- 怎样自行验证。

计划的质量，决定 Agent 能自主前进多远。

### **Agent → 人：交付物**

Agent 完成任务以后，不应该只报告：

已完成。

交付物应该主动降低人的验收成本。

至少应该包含：

- **改动摘要**：做了什么；
- **验证结果**：进行了哪些测试和检查，结果如何；
- **不确定点**：哪些地方仍然存在风险或假设；
- **决策记录**：执行过程中遇到了哪些问题，采用了什么默认方案；
- **重点验收项**：哪些地方真正需要人的判断；
- **最短验收路径**：人怎样用最少时间确认结果是否可接受。

对于 UI、App 等适合视觉验收的任务，可以进一步提供截图、录屏、构建结果或自动化测试结果。

**Agent 不只是负责完成任务，也负责降低人判断这个任务是否完成的成本。**

---

## **三、批次，而不是事件**

Agent 的工作可以是事件驱动的，但人的工作不应该被 Agent 的事件驱动。

**任务完成，不等于人应该立即介入。**

人的注意力由预先确定的验收窗口调度，而不是由机器什么时候完成来调度。

例如当前可以采用：

- 睡前：批量下发长期任务；
- 早晨：统一验收夜间结果，并下发新一轮；
- 下午 3 点：统一进行下一轮验收；
- 晚上：完成当天最后一轮探索、定义和下发。

具体时间以后可以调整。

真正固定的是：

**两次验收窗口之间，默认不查看正在执行的任务。**

一个任务即使已经完成，也可以在那里等待。

机器等待很便宜，人的注意力切换很贵。

---

## **四、人与机器形成流水线**

机器执行当前批次时，人不等待机器。

人的时间用于探索和定义未来任务。

因此形成两条并行流水线：

**机器：执行 N → 执行 N+1 → 执行 N+2**

**人：探索/定义 N+1 → 验收 N → 探索/定义 N+2 → 验收 N+1**

人不再围绕某一个 Agent 的生命周期工作，机器也不再围绕人的即时反馈工作。

双方通过：

**计划 → 执行 → 交付物 → 验收**

完成交接。

但只有当前置条件已经确定时，下一批任务才能提前进入定义和执行。依赖上一批结果才能决定方向的任务，继续停留在探索状态。

---

## **五、保护认知状态**

**探索只探索，定义只定义，验收只验收。**

探索时，不急着进入正式实现。

定义时，不重新无限发散。

验收时，不被某一个问题拖入即时修改。

执行时，人退出循环。

这样做的目的不是让流程看起来整齐，而是保护人的认知连续性。

当正在验收五个任务时，即使第二个任务发现严重问题，也先记录判断，继续完成这一批验收。

整个验收阶段完成以后，再统一处理失败项：

**需要探索的重新探索，需要重新定义的重新定义，然后形成下一批计划统一下发。**

**不要让一个任务劫持当前的认知阶段。**

---

## **六、失败进入下一轮，而不是立即修复**

Agent 的第一次执行不需要被视为必须一次成功。

失败本身也是新的信息。

当任务验收失败：

**失败结果 → 分析原因 → 必要时重新探索 → 重新定义 → 形成新计划 → 下一轮执行。**

不要默认进入：

**发现问题 → 马上让 AI 改 → 等它改 → 马上再看 → 再改。**

前者形成异步系统。

后者会重新退化成实时聊天。

这里的「下一轮」并不意味着必须等到下一个验收窗口。

当前批次验收完成、问题完成重新定义以后，就可以立即批量下发给机器。**需要等待的是人的认知阶段完成，而不是等待某个固定钟点。**

---

## **七、用验收能力限制并发**

机器能够同时运行多少任务，不应该决定我们同时启动多少任务。

真正的限制来自：

**人下一次能够验收多少结果。**

因此批次大小应该根据人的验收能力控制，而不是根据机器是否空闲控制。

一个简单的 v0.1 规则：

**一批任务预计产生的验收工作，不应该超过下一个验收窗口能够处理的容量。**

如果下午 3 点只准备投入 45 分钟验收，就不要因为机器还有空闲算力而制造两个小时的验收工作。

机器闲置不是问题。

**验收债务才是问题。**

---

## **八、当前运行方式**

v0.1 暂时采用一个简单制度：

**先探索和定义 → 批量下发 → 执行期间不看 → 固定窗口集中验收 → 验收只形成判断 → 失败项重新探索/定义 → 批量进入下一轮。**

睡眠天然作为最长的一次无人值守执行窗口。

白天 Agent 执行期间，人的主要工作不是监工，而是：

**探索未来的问题，形成下一批高确定性的计划。**

当前可以先尝试每天几个主要验收窗口，例如早晨、下午和晚上。具体时间和数量不是原则，根据真实运行情况调整。

---

## **九、v0.1 观察指标**

暂时不建立复杂 KPI，只记录几个能够帮助以后修改协议的指标：

- **一次验收通过率**：有多少任务第一次交付就可以接受；
- **返工轮数**：一个任务平均需要多少轮执行—验收；
- **中途阻塞次数**：Agent 有多少次因为无法自行判断而需要人介入；
- **验收耗时**：人的时间最终有多少花在判断机器结果上。

同时观察一个简单现象：

**我今天被 Agent 打断了多少次？**

这些数据不是为了考核 Agent，而是为了寻找工作协议本身的问题。

如果一次通过率低，可能是定义质量不足。

如果返工轮数高，可能是验证闭环不足。

如果中途阻塞多，可能是计划边界和默认策略不足。

如果验收耗时持续增长，说明系统正在产生认知背压。

---

## **一句话**

**人按认知阶段工作，机器按任务并发工作；通过计划与交付物异步交接，通过批量验收完成判断闭环。**

---

v0.1
